import { createTheme } from "@mui/material";

export const light_theme = createTheme({

    palette: {
        mode: "light",
        primary: {
            main: "#1565c0",
        },
        secondary: {
            main: "#d32f2f",
        },
    },
});
