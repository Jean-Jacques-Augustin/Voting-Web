import { createTheme } from "@mui/material";

export const dark_theme = createTheme({

    palette: {
        mode: "dark",
        primary: {
            main: "#1565c0",
        },
        secondary: {
            main: "#d32f2f",
        },
    },
});
